# cargo-deny configuration for BraiNIX
# Only check dependencies for the bare-metal target.

[graph]
targets = [{ triple = "x86_64-unknown-none" }]

[licenses]
allow = ["MIT", "Apache-2.0", "Unicode-3.0"]
private = { ignore = true }

[bans]
multiple-versions = "warn"

[advisories]
ignore = []
