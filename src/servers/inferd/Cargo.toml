[package]
name = "brainix-inferd"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "inferd: the confined tenant's manifest and per-session KV binding (P3-T7)"

[dependencies]
brainix-bsp = { path = "../../bsp" }

[lints]
workspace = true
