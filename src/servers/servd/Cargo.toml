[package]
name = "brainix-servd"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "servd: the serving front end's fixed session-slot pool and admission control (P2-T4)"

[dependencies]
brainix-bsp = { path = "../../bsp" }

[lints]
workspace = true
