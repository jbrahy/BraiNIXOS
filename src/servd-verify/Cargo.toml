[package]
name = "brainix-servd-verify"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Kani proof harnesses for bounded admission (INV-SERVE-003)"

[dependencies]
brainix-bsp = { path = "../bsp" }
brainix-servd = { path = "../servers/servd" }

[lints]
workspace = true
