[package]
name = "brainix-bootloader"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
multiboot2 = { version = "0.24.1", default-features = false }

[lints]
workspace = true
