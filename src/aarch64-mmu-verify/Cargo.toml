[package]
name = "brainix-aarch64-mmu-verify"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Kani proof that no aarch64 descriptor is both writable and executable (INV-MEM-003)"

[dependencies]
brainix-aarch64-mmu = { path = "../aarch64-mmu" }

[lints]
workspace = true
