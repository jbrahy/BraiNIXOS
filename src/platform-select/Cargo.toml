[package]
name = "brainix-platform-select"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Device selection from ADT compatible strings, failing closed on the unknown (AS-2a)"

[dependencies]

[lints]
workspace = true
